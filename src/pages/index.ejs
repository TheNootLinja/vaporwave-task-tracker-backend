<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <h1>Home Page</h1>

    <main>
        <% task.forEach(task => { %>
            <ul>
                <li><h1><a href="/<%= task.id %> "><%= task.name %> </a></h1></li>
                <li><%= task.date %> </li>
                <li><p><%= task.description %></p></li>
            </ul>
        <% }) %> 
    </main>
</body>
</html>