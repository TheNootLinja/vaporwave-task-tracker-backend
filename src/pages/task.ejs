<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task</title>
</head>
<body>
    <main>
        <h1>Task</h1>
        <h1><%= task.name %> </h1>
        <p><%= task.description %> </p>

        <div>
            <!-- The form below is setup to delete the currently viewed
            task using the POST route and the action of /delete -->
            <form method='POST' action='/delete'>
                <div>
                    <input type="hidden" value="<%= task.id %>" name="taskId">
                    <button>Delete</button>
                </div>
            </form>
        </div>
    </main>
</body>
</html>